#include <stdio.h>

int main()
{
    int n,i,a[100];
    printf("Enter the number range:\n");
    scanf("%d",&n);
    a[0]=-1;
    a[1]=1;
    printf("\n Fibonacci series:");
    for(i=2;i<=n+1;i++){
        a[i]=a[i-1]+a[i-2];
        printf("%5d",a[i]);
    }
    return 0;
