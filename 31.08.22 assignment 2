#include<stdio.h>
#include<string.h>
int main() 
{
  char str[100];
  puts("enter the string:");
  gets(str);
  int i;
  for(int i=0;str[i]!='\0';i++)
  {
      if(str[i]=='a') 
      {
          str[i]=toupper(str[i]);
      }
  }
  printf("\n%s", str);
  return 0;
}
