#include <stdio.h>

int main()
{
    char str[100];
    int i;
    printf("Enter the string:");
    gets(str);
    for(i=0;str[i]!='\0';i++)
    {
        if(i%2==0)
        str[i]=tolower(str[i]);
        else
        str[i]=toupper(str[i]);
    }
    printf("\n%s",str );
    return 0;
}
