#include <stdio.h>

int main()
{
    int i,n,sum=0,missing;
    scanf("%d",&n);
    int a[n-1];
    for(i=0;i<n-1;i++){
    scanf("%d",&a[i]);
    sum=sum+a[i];
}
   missing = (n*(n+1))/2 - sum;
printf("Missing number is %d",missing);
    return 0;
}
