#include <stdio.h>

int main() {
    int n,result;
    printf("Enter a number:");
    scanf("%d",&n);
    result=fact(n);
    printf("Factorial is %d",result);
    
}
int fact(int n)
{
    int i,fact=1;
    for(i=1;i<=n;i++)
    {
        fact=fact*i;
    }
    return(fact);
}
